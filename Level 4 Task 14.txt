#include <iostream>
using namespace std;
int main() {
    int n, r=0, t;
    cout << "Ededi daxil edin: ";
    cin >> n;
    t=n;
    while (t!=0) {
        r=r*10+t%10;
        t/=10;
    }
    if (r==n)
        cout << "Eded palindromdur.";
    else
        cout << "Eded palindrom deyil.";
    return 0;
}
